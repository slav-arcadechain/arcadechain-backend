# deploying functions
to deploy run:
```
firebase deploy --only functions
```
from `/functions` directory

# local development
run :
```
firebase emulators:start --only functions
```
from `/functions` directory 